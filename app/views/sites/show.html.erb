<h1>Policies for <%= @site.name.capitalize %>:</h1>

<% if @site.policies.any? %>
<ul class="policies">
	<% @site.policies.each do |policy| %>
		<li><%= link_to policy.name, policy_path(policy) %></li>
	<% end %>
</ul>
<% end %>
