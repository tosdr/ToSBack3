<h1>All versions of <%= @policy.name %></h1>
<h3><%= link_to "Back to current policy...", policy_path(@policy) %></h3>
<div class="row">
  <div class="span3">
	<ul class="policy_versions">
      <% @versions.each do |v| %>
        <li><%= link_to v.created_at.to_date.to_formatted_s(:long), [@policy, v] %></li>
      <% end %>
    </ul>
  </div>
<!-- TODO: make this look better/change content on this page to have version stats or something -->
  <div class="span9">
	<h3>Here's the list of versions that we have archived!</h3>
	<p>Choose a version on the left to see the archived copy, and then compare what's been changed.</p>
  </div>
</div>
<%= will_paginate @versions %>